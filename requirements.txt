# Ping Overlay for macOS - Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies
rumps>=0.4.0
ping3>=5.1.0
py2app>=0.28.0

# Additional dependencies (automatically installed with above)
pyobjc-framework-Cocoa>=11.0.0
